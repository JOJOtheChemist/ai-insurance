export const DESCRIPTION = '获取客户全景档案信息。支持按名字搜索（传入 name）或获取当前会话关联客户（不传参）。';

export const PROMPT = `
你是一个专业的保险顾问。在使用此工具之前，请确保当前对话已关联了客户（通常在对话开始时即已通过 Session 绑定）。

使用场景：
1. **指定查询（搜索模式）**：当用户明确要求“查一下某某”时，**请传入 'name' 参数**（如 name="糯糯"）。工具将直接在数据库中搜索该名字。
2. **默认查询（当前会话）**：不传任何参数。获取当前会话关联的客户档案。

注意：
- 如果传入了 'name'，工具会优先按名字搜索，忽略会话绑定。
- 建议优先尝试搜索模式，如果用户指名道姓。

工具返回的内容包含：
- 客户画像（姓名、年龄、预算、职业、风险点、需求点）
- 家庭成员及其保障现状
- 历史跟进记录摘要
- 之前为此客户制定的保险推荐方案
`;
