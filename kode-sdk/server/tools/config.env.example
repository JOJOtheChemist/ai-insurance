# ============================================================
# 工具配置环境变量示例
# ============================================================
# 
# 使用说明：
# 1. 复制此文件内容到项目根目录的 .env 文件
# 2. 根据实际情况修改配置值
# 3. 不要将包含真实密钥的 .env 文件提交到 Git
#
# ============================================================

# ------------------------------------------------------------
# 后端 API 配置
# ------------------------------------------------------------

# 主后端 API 地址
# 开发环境：http://localhost:8000
# 生产环境：https://api.your-domain.com
BACKEND_API_URL=http://localhost:8000

# API 请求超时时间（毫秒）
API_TIMEOUT=10000

# ------------------------------------------------------------
# JWT 认证配置
# ------------------------------------------------------------

# 默认测试用户 JWT Token（可选，仅用于开发测试）
# 在生产环境中，token 应该从前端传入
USER_JWT_TOKEN=

# Token 过期检查缓冲时间（毫秒）
TOKEN_EXPIRY_BUFFER=300000

# ------------------------------------------------------------
# GLM AI 配置（用于自然语言解析）
# ------------------------------------------------------------

# GLM API Key（必填）
# 从智谱 AI 平台获取：https://open.bigmodel.cn/
GLM_API_KEY=your_glm_api_key_here

# GLM API 地址
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# 使用的 GLM 模型
# 可选值: glm-4.5-air, glm-4-flash, glm-4, glm-3-turbo
GLM_MODEL=glm-4.5-air

# GLM 温度参数（0.0-1.0，控制输出随机性）
GLM_TEMPERATURE=0.7

# GLM 最大 token 数
GLM_MAX_TOKENS=1000

# ------------------------------------------------------------
# 工具通用配置
# ------------------------------------------------------------

# 默认最大重试次数
TOOL_MAX_RETRY=3

# 重试间隔时间（毫秒）
TOOL_RETRY_INTERVAL=1000

# 是否启用调试日志（true/false）
TOOL_DEBUG=false

# ------------------------------------------------------------
# Kode AI 配置
# ------------------------------------------------------------

# Kode AI 认证 Token
# 从 Kode AI 平台获取
ANTHROPIC_AUTH_TOKEN=your_kode_auth_token_here

# Kode Agent 服务端口
PORT=3000

# ------------------------------------------------------------
# 配置说明
# ------------------------------------------------------------
#
# 1. 后端 API 地址配置：
#    - 本地开发：http://localhost:8000
#    - Docker 部署：http://backend:8000
#    - 生产环境：https://api.your-domain.com
#
# 2. GLM API Key 获取：
#    - 访问：https://open.bigmodel.cn/
#    - 注册账号并创建 API Key
#    - 复制 API Key 到 GLM_API_KEY
#
# 3. JWT Token 说明：
#    - USER_JWT_TOKEN 仅用于开发测试
#    - 生产环境应从前端上下文获取
#    - Token 格式：Bearer xxxxx.yyyyy.zzzzz
#
# 4. 调试模式：
#    - 设置 TOOL_DEBUG=true 查看详细日志
#    - 用于排查 API 调用问题
#
# ============================================================

