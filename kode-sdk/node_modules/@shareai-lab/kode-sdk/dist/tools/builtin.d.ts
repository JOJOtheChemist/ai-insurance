import { Tool } from './fs';
import { AgentTemplate } from './task';
export declare const builtin: {
    fs(opts?: {
        workDir?: string;
    }): Tool[];
    bash(opts?: {
        allow?: RegExp[];
        block?: RegExp[];
        approval?: boolean;
    }): Tool[];
    task(opts?: {
        subAgents?: AgentTemplate[];
    }): Tool;
};
